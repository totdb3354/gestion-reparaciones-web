save-exact=true
# openapi-typescript@7.13.0 declara peer typescript ^5.x; el proyecto usa 6.0.3 (sin problemas reales,
# solo el rango del peerDependency no se ha actualizado todavía). Sin esto, `npm ci` falla en CI.
legacy-peer-deps=true
